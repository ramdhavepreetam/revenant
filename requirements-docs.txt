# Documentation toolchain (MkDocs + Read the Docs theme + mkdocstrings autodoc).
# The readthedocs theme ships with mkdocs itself — no separate theme package.
mkdocs>=1.6
mkdocstrings>=1.0
mkdocstrings-python>=2.0
