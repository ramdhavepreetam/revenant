[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nerva-core"
version = "0.6.1"
description = "Offline local-LLM layer (Ollama / OpenAI-compatible) with SQLite storage and profiles."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Preetam Ramdhave" }]
keywords = ["llm", "ollama", "local", "offline", "agent"]
dependencies = []
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.11",
  "Operating System :: OS Independent",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

# Optional: a real tokenizer for accurate context-budget estimates. Absent by
# default so the core stays zero-dependency and fully offline; estimate_tokens
# transparently falls back to a word-count heuristic when tiktoken isn't installed.
[project.optional-dependencies]
tokenizer = ["tiktoken>=0.5"]

[project.urls]
Homepage = "https://github.com/ramdhavepreetam/revenant"
Repository = "https://github.com/ramdhavepreetam/revenant"

[tool.setuptools.packages.find]
where = ["src"]
