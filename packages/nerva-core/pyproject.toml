[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nerva-core"
version = "0.1.0"
description = "Offline local-LLM writer + memory/profiles/storage (Ollama)."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
